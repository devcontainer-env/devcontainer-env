[package]
name = "devcontainer-env"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.0", features = ["derive", "env"] }

[dev-dependencies]
assert_cmd = "2.2.0"
clap = "4.6.0"
